"""Scenes bounded context (Slice α5c).

Holds the :class:`~app.domain.scenes.scene.Scene` aggregate — the ordered,
editable unit of a project's storyboard. See
``docs/domain/SCENE_AGGREGATE.md`` for the full aggregate model.
"""
