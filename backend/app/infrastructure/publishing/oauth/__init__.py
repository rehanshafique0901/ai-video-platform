"""Per-platform OAuth clients (connection mechanism, not the upload API).

α8.6a ships only the deterministic :class:`MockSocialOAuthClient`. The real YouTube OAuth
client lands with its destination upload adapter in α8.6c (OQ1).
"""
