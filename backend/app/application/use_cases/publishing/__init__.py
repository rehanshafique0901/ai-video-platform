"""Publishing use cases (α8.6a Account Connections).

The connection lifecycle: begin an OAuth connection, complete it (exchange + encrypt +
store), list connected accounts, and revoke one. No publish execution, upload, or
scheduling — those are α8.6b / α8.6c.
"""
